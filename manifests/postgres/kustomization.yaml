apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

# postgres-secret holds the superuser credentials and is applied by hand.
resources:
  - service.yaml
  - statefulset.yaml
