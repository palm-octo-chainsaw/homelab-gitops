apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

# postgres-secret holds the superuser credentials. Sealed rather than hand-applied
# since the sealed-secrets controller went in.
resources:
  - service.yaml
  - statefulset.yaml
  - sealed/postgres-secret.sealed.yaml
