apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

# crypto-bot-secret holds the Telegram token and exchange API keys; applied by hand.
resources:
  - priorityclass.yaml
  - pvc.yaml
  - deployment.yaml
  - restart-cronjob.yaml
